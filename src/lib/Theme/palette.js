const primaryLight = '#f5f5f5'
const primary = '#2fb3fa'
const primaryDark = '#1682bc'
const inputTextColor = '#606060'

export default {
  themePalette: {
    primary: {
      light: primaryLight,
      main: primary,
      dark: primaryDark,
    },
    // secondary: {
    //   light: defaultTheme.palette.secondary.light,
    //   main: defaultTheme.palette.secondary.main,
    //   dark: defaultTheme.palette.secondary.dark,
    // },
    // error: red.A400,
  },
  primaryLight,
  primary,
  primaryDark,
  inputTextColor
}